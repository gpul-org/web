---
import PageLayout from "../layouts/PageLayout.astro";
import PageTitle from "../components/PageTitle.astro";
import Card from "../components/Card.astro";
import { Plus } from "lucide-astro";
import { getCollection, getEntry } from 'astro:content';

const nextEvents = await getCollection('events', ({ data }) => data.status === "upcoming");
const pastEvents = await getCollection('events', ({ data }) => data.status !== "upcoming");
---

<PageLayout 
  title="Eventos - GPUL" 
  description="Descubre os próximos eventos, charlas, cursos e actividades organizadas polo Grupo de Programadores e Usuarios de Linux (GPUL)."
>
  <PageTitle 
    title="Eventos GPUL" 
    subtitle="Participa nas nosas actividades, aprende novas tecnoloxías e conecta coa comunidade"
  />

  <!-- Stats -->
  <section class="py-12 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 md:grid-cols-4 gap-8 text-center">
        <div>
          <div class="text-3xl font-bold text-blue-600 mb-2">200+</div>
          <div class="text-gray-600">Participantes en HackUDC</div>
        </div>
        <div>
          <div class="text-3xl font-bold text-green-600 mb-2">50+</div>
          <div class="text-gray-600">Eventos anuais</div>
        </div>
        <div>
          <div class="text-3xl font-bold text-purple-600 mb-2">1000+</div>
          <div class="text-gray-600">Estudantes formados</div>
        </div>
        <div>
          <div class="text-3xl font-bold text-orange-600 mb-2">26</div>
          <div class="text-gray-600">Anos de experiencia</div>
        </div>
      </div>
    </div>
  </section>

  <!-- Próximos eventos -->
  <section class="py-16 bg-gray-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <h2 class="text-3xl font-bold text-gray-900 mb-4">Próximos Eventos</h2>
        <p class="text-xl text-gray-600">
          Non te perdas as nosas próximas actividades
        </p>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {nextEvents.map((event) => (
          <div class="relative">
            <Card
              title={event.data.title}
              excerpt={event.data.excerpt}
              date={event.data.date}
              href={event.data.href}
              tags={event.data.tags}
            />
            <div class="absolute top-4 right-4">
              <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                Próximamente
              </span>
            </div>
          </div>
        ))}
      </div>
      
      <!-- CTA -->
      <div class="text-center mt-12">
        <a 
          href="/contact" 
          class="inline-flex items-center px-6 py-3 bg-blue-600 text-white font-medium rounded-lg hover:bg-blue-700 transition-colors"
        >
          Propón un evento
          <Plus class="ml-2 w-5 h-5" />
        </a>
      </div>
    </div>
  </section>

  <!-- Eventos destacados -->
  <section class="py-16">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <h2 class="text-3xl font-bold text-gray-900 mb-4">Os Nosos Fregados</h2>
        <p class="text-xl text-gray-600">
          Grandes eventos que organizamos cada ano
        </p>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
        <!-- HackUDC -->
        <div class="bg-gradient-to-r from-blue-500 to-purple-600 rounded-2xl p-8 text-white">
          <h3 class="text-2xl font-bold mb-4">HackUDC</h3>
          <p class="text-blue-100 mb-6">
            O maior hackathon de Galicia. 48 horas de innovación, creatividade e programación coa mellor comunidade tech do noroeste.
          </p>
          <div class="flex items-center space-x-4">
            <span class="bg-white/20 px-3 py-1 rounded-full text-sm">Febreiro</span>
            <span class="bg-white/20 px-3 py-1 rounded-full text-sm">200+ participantes</span>
          </div>
        </div>
        
        <!-- AtlánticaConf -->
        <div class="bg-gradient-to-r from-green-500 to-blue-500 rounded-2xl p-8 text-white">
          <h3 class="text-2xl font-bold mb-4">AtlánticaConf</h3>
          <p class="text-green-100 mb-6">
            Conferencia de tecnoloxía e innovación do noroeste peninsular. Conectamos profesionais, estudantes e empresas.
          </p>
          <div class="flex items-center space-x-4">
            <span class="bg-white/20 px-3 py-1 rounded-full text-sm">2027</span>
            <span class="bg-white/20 px-3 py-1 rounded-full text-sm">Próximamente</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Eventos pasados -->
  <section class="py-16 bg-gray-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex items-center justify-between mb-12">
        <h2 class="text-3xl font-bold text-gray-900">Eventos Anteriores</h2>
        <a href="/blog" class="text-blue-600 hover:text-blue-700 font-medium">
          Ver todas as crónicas →
        </a>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {pastEvents.map((event) => (
          <div class="relative">
            <Card
              title={event.data.title}
              excerpt={event.data.excerpt}
              date={event.data.date}
              href={event.data.href}
              tags={event.data.tags}
            />
            <div class="absolute top-4 right-4">
              <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-800">
                Finalizado
              </span>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>
</PageLayout>
